import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF7A8E2E);
  static const Color primaryDark = Color(0xFF5C6E1E);
  static const Color primaryLight = Color(0xFF9AAB3C);

  static const Color loginTop = Color(0xFFB8CB38);
  static const Color loginBottom = Color(0xFF4A6820);

  // Fundo branco puro — igual ao Graint
  static const Color background = Colors.white;
  static const Color surface = Colors.white;

  // Barra de seção (NOTAS, TRANSPORTE, CLASSIFICAÇÃO)
  static const Color sectionBar = Color(0xFF9E9E9E);

  // Bordas e underlines
  static const Color border = Color(0xFFE0E0DD);
  static const Color divider = Color(0xFFE8E8E5);
  static const Color underline = Color(0xFFCCCCCC);

  // Textos
  static const Color textPrimary = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFAAAAAA);
  static const Color textGreenBold = Color(0xFF3A5410);

  static const Color danger = Color(0xFFC94A4A);
}
